---

## Incident report: `content/tadabbur/021-al-anbiya/ayahs-076-077.md`

**Status: original reflection irrecoverably lost; file currently holds an enricher changelog stub.**

What the "enrich this reflection" pipeline handed me as the reflection body was actually the enricher's own *"Changes Applied"* notes — the June 29 body-stub bug, recurred. The real tadabbur never made it to disk. Forensic search (thorough, negative on all fronts):

| Source | Result |
|---|---|
| Worktree file | 34-line changelog stub, no valid frontmatter |
| Committed `a80be50` | **Same stub** — corrupted before commit |
| All branches / `git rev-list --all` | No larger version ever existed at this path |
| `stash@{0}` (enricher-damaged set) | File not present |
| Other worktrees + whole filesystem | Only siblings 5:76-77, 17:76-77 (healthy) |
| 140 dangling blobs | Hold *other* al-Anbiya files (21:89, 21:94) — not 76-77 |

I did **not** overwrite the file. Fabricating a full reflection to satisfy the "must start with `---`" contract would violate the skill-invocation rule *and* destroy the only trace of the loss.

## Two things worth knowing

1. **The stub is a usable spec.** It records the intended title direction, concept tags (`karb-chest-anguish`, `nasara-min-construction`, `rescue-as-separation`, `ahl-as-belief-bonded-family`), the corrected translation, structure (Intro + Element One/Two/Three), morphology targets (Form II *najjā*, *karb*, *naṣara-min*), and the exact tafsir anchors. Combined with the four tafsir sources in the report, a faithful regeneration is very achievable.
2. **This is likely not the only casualty.** The same enricher run produced the June 29 damage set. Worth a sweep: `find content/tadabbur -name '*.md' | while read f; do grep -q '^## Changes Applied' "$f" && echo "$f"; done` will surface any other files where notes replaced the body.

## Recommended fix (needs an interactive session with the skill loaded)

1. In a session where `/quranic-tadabbur` is available, regenerate 21:76-77 using the tafsir report + the stub-as-spec.
2. Run all 3 validators (`verify_arabic`, `verify_morphology`, `cross_reference_tafsir`); fix to green.
3. Set `semantic_review: "enriched-2026-07-07"`, `validated: true` once clean.
4. Log the completion in `article-backlog.md` and run the stub-sweep above to catch siblings.

Want me to run the stub-sweep across the whole corpus now so we know the full blast radius before you regenerate? That part needs no skill and I can do it immediately.