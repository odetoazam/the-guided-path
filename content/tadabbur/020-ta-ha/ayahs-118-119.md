---` at the top is the only frontmatter-like element, and it's empty.

I cannot "enrich an existing reflection" when the reflection isn't in the input. To produce a complete markdown file starting with `---`, I would have to **fabricate the entire tadabbur from scratch** — invent the frontmatter fields, reconstruct the four-element structure and theme prose from the changelog's descriptions, and re-author morphology tags I was told to "preserve exactly as-is" but was never given. That directly violates the hard rules of this task (don't fabricate, preserve existing voice/morphology) and the project's critical rule against generating Quranic content outside the `/quranic-tadabbur` skill. This is precisely the failure mode of the semantic-enrich incident already on record.

The format instruction says start with `---`. Content integrity for sacred text overrides it. I won't fake a file.

**What actually needs to happen for `content/tadabbur/020-ta-ha/ayahs-118-119.md`:**

1. **Recover the real reflection body.** The changelog references it in detail (frontmatter with `semantic_review`/`validated`/`validation_date`, "The First/Second/Third Element," "Theme One/Two," morphology tags, the cross-pairing passage). That real content exists somewhere — almost certainly in git history before the changelog overwrote the body:

```
git log --oneline -- content/tadabbur/020-ta-ha/ayahs-118-119.md
git show <commit-before-corruption>:content/tadabbur/020-ta-ha/ayahs-118-119.md
```

2. **Check whether the pipeline wrote the changelog into the file body** (same class of bug as the enricher incident that truncated 127 bodies) — if so, other files in this batch may be corrupted the same way and should be audited.

3. **Re-run this enrichment task against the recovered reflection.** Once I have the genuine body + frontmatter + morphology tags, the Part 1 fixes and Part 2 tafsir deepening are all well-supported by the provided sources (the al-Tabari qirāʾāt ikhtilāf on 20:119, al-Muyassar's `ḥarr al-shams` and positive framing, al-Tabari's `an`-clause grammar on 20:118) and I can do the pass cleanly.

Want me to run the `git show` recovery now so we can get the real file back, then I'll do the enrichment on the actual content?